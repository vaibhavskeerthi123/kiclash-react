BACKGROUND IMAGES (placeholders)
================================
Drop your own JPEGs here to set the backgrounds:
  public/backgrounds/title.jpg    -> loading + title screen
  public/backgrounds/select.jpg   -> character select screen
If a file is missing, the dark gradient still shows (nothing breaks).
